import React from 'react'; // eslint-disable-line no-unused-vars

import './BoxSizingReset.scss'; // eslint-disable-line local/import-css-as-css


/**
 * Stateless Box Sizing Reset component. Doesn't really need to do anything; if you
 * pull this file in anywhere in your project the CSS will be included and
 * since it's all globals it should Just Work.
 */
export const BoxSizingReset = () => null;
