/**
 * This folder contains functions and components necessary for freezing a page (i.e. preventing it from scrolling).
 *
 * Typical use cases include:
 * - Modals
 * - Custom select boxes on mobile.
 *
 * To freeze a page using this component, simply render <PageFreeze> within another component.
 * To unfreeze the page, remove the component.
 *
 */
import {PageFreeze} from './PageFreeze';

export {
  PageFreeze,
};
